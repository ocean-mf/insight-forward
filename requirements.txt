# ─────────────────────────────────────────────────────────────────────────────
# UVOS Pipeline: RAFT + SAM 2 — Python dependencies
# ─────────────────────────────────────────────────────────────────────────────
# Install with:
#   pip install -r requirements.txt
#
# Note: PyTorch and TorchVision must be installed separately to match your
# CUDA version.  On the DLAMI (AWS Deep Learning AMI, Ubuntu 24.04):
#   source /opt/pytorch/bin/activate   # PyTorch 2.9.1+cu130 already present
#
# All other packages below are available via pip.
# ─────────────────────────────────────────────────────────────────────────────

# Core ML / Vision
# torch>=2.1.0        # installed in the DLAMI venv — do not reinstall
# torchvision>=0.16   # installed in the DLAMI venv — do not reinstall

# SAM 2 (Meta)
sam2>=1.1.0

# Optical-flow transforms + model weights are part of torchvision

# Computer Vision
opencv-python-headless>=4.8.0

# Array / Image processing
numpy>=1.24.0
Pillow>=10.0.0

# Progress bars
tqdm>=4.65.0

# HuggingFace Hub (checkpoint auto-download for SAM 2)
huggingface_hub>=0.20.0

# Visualisation (optional, used by metrics.py summary plotting)
matplotlib>=3.7.0

# SAM 2 transitive dependencies (installed automatically with sam2)
# hydra-core>=1.3.0
# omegaconf>=2.3.0
# iopath>=0.1.9
